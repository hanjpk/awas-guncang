# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.buildlog/
.history
.svn/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

*.env

# ==> Firebase
# Ignore sensitive Firebase configuration files.
# These files are generated by Firebase and contain project-specific API keys and identifiers.
# Each developer should generate their own by following the Firebase setup instructions.

# Firebase CLI configuration
/firebase.json

# Android
/android/app/google-services.json

# iOS
/ios/
/ios/Runner/GoogleService-Info.plist
/ios/firebase_app_id_file.json

# Web, macOS, Windows, Linux (generated by FlutterFire CLI)
# It's best practice to ignore this file to avoid committing project-specific keys.
# Update the path if you place this file elsewhere in your project.
/lib/controller/firebase_options.dart

# ==> Keystores & Signing Certificates
# Ignore private keys and signing certificates.
# These should be managed securely and should not be in version control.
*.jks
*.keystore
*.p12
*.cer
/android/key.properties